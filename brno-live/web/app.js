const $=(selector)=>document.querySelector(selector);
const weatherNames={0:'Jasno',1:'Převážně jasno',2:'Polojasno',3:'Zataženo',45:'Mlha',48:'Mrznoucí mlha',51:'Slabé mrholení',53:'Mrholení',55:'Silné mrholení',61:'Slabý déšť',63:'Déšť',65:'Silný déšť',71:'Slabé sněžení',73:'Sněžení',75:'Silné sněžení',80:'Přeháňky',81:'Dešťové přeháňky',82:'Silné přeháňky',95:'Bouřka'};
function time(value){return new Date(value).toLocaleTimeString('cs-CZ',{hour:'2-digit',minute:'2-digit'})}
function aqiName(value){if(value<=20)return'Dobrá';if(value<=40)return'Přijatelná';if(value<=60)return'Zhoršená';if(value<=80)return'Špatná';if(value<=100)return'Velmi špatná';return'Extrémně špatná'}
async function load(){const response=await fetch('/api/data');const data=await response.json();if(!response.ok)throw Error(data.error);const w=data.weather.current,a=data.air.current,d=data.weather.daily;$('#temp').textContent=Math.round(w.temperature_2m);$('#feels').textContent=Math.round(w.apparent_temperature);$('#cloud').textContent=w.cloud_cover;$('#wind').textContent=Math.round(w.wind_speed_10m);$('#gust').textContent=Math.round(w.wind_gusts_10m);$('#rain').textContent=w.precipitation.toFixed(1);$('#condition').textContent=weatherNames[w.weather_code]||'Proměnlivé počasí';$('#updated').textContent='aktualizováno '+new Date(data.updated*1000).toLocaleTimeString('cs-CZ');$('#aqi').textContent=Math.round(a.european_aqi);$('#aqi-title').textContent=aqiName(a.european_aqi);$('#pm25').textContent=a.pm2_5.toFixed(1)+' µg/m³';$('#pm10').textContent=a.pm10.toFixed(1)+' µg/m³';$('#no2').textContent=a.nitrogen_dioxide.toFixed(1)+' µg/m³';$('#o3').textContent=a.ozone.toFixed(1)+' µg/m³';$('#sunrise').textContent=time(d.sunrise[0]);$('#sunset').textContent=time(d.sunset[0]);const hours=Math.floor(d.daylight_duration[0]/3600),minutes=Math.round(d.daylight_duration[0]%3600/60);$('#daylight').textContent=`${hours} h ${minutes} min`;const start=data.weather.hourly.time.findIndex(x=>x>=data.weather.current.time),end=start+24,labels=data.weather.hourly.time.slice(start,end).map(time);new Chart($('#forecastChart'),{data:{labels,datasets:[{type:'line',label:'Teplota °C',data:data.weather.hourly.temperature_2m.slice(start,end),borderColor:'#d44835',yAxisID:'y',tension:.25},{type:'bar',label:'Srážky mm',data:data.weather.hourly.precipitation.slice(start,end),backgroundColor:'#3f83a8aa',yAxisID:'rain'},{type:'line',label:'Vítr km/h',data:data.weather.hourly.wind_speed_10m.slice(start,end),borderColor:'#08736a',yAxisID:'wind',pointRadius:0}]},options:{responsive:true,maintainAspectRatio:false,interaction:{mode:'index',intersect:false},scales:{y:{position:'left',title:{display:true,text:'°C'}},rain:{position:'right',min:0,title:{display:true,text:'mm'},grid:{drawOnChartArea:false}},wind:{display:false}}}})}
load().catch(()=>{$('#updated').textContent='data dočasně nedostupná'});
